interface:
  display_name: "Impeccable"
  short_description: "Explicit full interface design workflow"
  default_prompt: "Use $impeccable for this explicitly selected interface design workflow."
policy:
  allow_implicit_invocation: false
