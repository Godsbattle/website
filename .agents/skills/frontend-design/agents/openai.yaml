interface:
  display_name: "Frontend Design"
  short_description: "Explicit creative frontend direction"
  default_prompt: "Use $frontend-design to create this explicitly requested greenfield or marketing interface."
policy:
  allow_implicit_invocation: false
