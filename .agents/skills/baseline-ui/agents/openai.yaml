interface:
  display_name: "Baseline UI"
  short_description: "Explicit broad Tailwind UI baseline"
  default_prompt: "Use $baseline-ui for this explicitly requested broad Tailwind baseline pass."
policy:
  allow_implicit_invocation: false
